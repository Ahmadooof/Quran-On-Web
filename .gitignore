# dependencies
node_modules/

# secrets — QF_CLIENT_ID / QF_CLIENT_SECRET live here, never commit them
.env
*.env
.env.*
!.env.example
public/data/.env

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# python scratch
__pycache__/
*.py[cod]
venv/
.venv/

# build / temp
dist/
build/
tmp/
*.bak

# QCF mushaf page fonts: 1208 files, ~140 MB. Fetched with `npm run fetch:fonts`.
public/fonts/v1/
public/fonts/v2/

# Downloaded reference texts, used only by tests/check-data.js
reference/

# QCF verification
# The page photographs: 173 MB of camera captures, and the one thing here that
# is genuinely yours rather than derived. Kept out of git and off the network.
Verification/PhysicalQuran/
# scratch renders
Verification/_*.png
